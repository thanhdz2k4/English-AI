{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd ../.. && mvn clean install -DskipTests && cd backend/ai-service && mvn package -DskipTests"
  },
  "deploy": {
    "startCommand": "java -jar target/ai-service-1.0.0.jar",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
